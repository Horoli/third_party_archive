part of third_party_archive;

class WidgetSkillGemInfo extends StatelessWidget {
  const WidgetSkillGemInfo({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // Text(skillGem.description),
        // Text(skillGem.tags.join(', ')),
      ],
    );
  }
}
