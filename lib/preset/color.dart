import 'package:flutter/material.dart';

Color OVER_FORTY = Colors.red.withValues(alpha: 0.3);
Color OVER_TWENTY = Colors.green.withValues(alpha: 0.3);
Color OVER_TEN = Colors.blue.withValues(alpha: 0.3);
Color OVER_FOUR = Colors.purple.withValues(alpha: 0.3);
Color DEFAULT = Colors.white.withValues(alpha: 0.3);
